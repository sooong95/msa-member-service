server:
  port: 8080

spring:
  application:
    name: member
  # profiles:
  #  active: docker # ?? ???? ????? docker? ??. ?, application-docker.yml ? ??? ? ??? ?? ??.?? ?? ?? (local, dev, prod ?)? ??? ? ??.
  database:
    url: jdbc:mysql://mysql-member:3306/member_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: member
    password: memberpw
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true # ??? ????? ?? ??? ???
          lower-case-service-id: true
    zookeeper:
      connecting-string: localhost:2181 # zookeeper ??
      discovery:
        enabled: true # Zookeeper ? ??? ??? ????? ??? ???? ?, ? ??? ???, Spring Boot? Zookeeper? ??? ???? ??? ??/??? ?? ???.
        register: true # ???(member-service)? Zookeeper ? ????? ??.
        instance-id: ${spring.application.name}:${random.value} # Zookeeper? ??? ?, ?member-service:asdf1234? ?? ?? ???? ??

  kafka:
    bootstrap-servers: kafka:9092
    consumer:
      group-id: member-group # Kafka ???? ?? ID. ?? ?? ID? ?? ????? ???? ?? ??? ??.??? member-group ??? ??? ???? ????, member ????? ???? ???.
      auto-offset-reset: earliest # ???? ?? ?? ?, ?? ?? ????? ???, ??? ?? ?? ????? ??? ??. earliest: ?? ?? ID? ??? ???? ??? ?? ??? ????? ??. latest: ???? ?? ?? ???? ??.
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer # Kafka ???? key ? ????(??? ? ??)? ? ??? ???. ???? ??????? ?.
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer # Kafka ???? value ? ????? ???? ??????. ????? JSON ??? ???? ??? ? ??.

management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    export:
      prometheus:
        enabled: true
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans

# ?? ?? ??
#docker
#????
# ??